# Product Requirements Document (PRD)

**Project Name:** Smart Race Pacer (LINE LIFF Edition)
**Version:** 2.0 (Final MVP)
**Date:** November 23, 2025

## 1\. Project Overview

**Description:**
Web Application รูปแบบ Single Page Application (SPA) ที่ฝังใน LINE LIFF สำหรับคำนวณแผนการวิ่งแบบ **Negative Split** (เริ่มช้า-กลางนิ่ง-จบคอมโบ) โดยเน้น UX ที่ลื่นไหล สวยงาม และให้ผลลัพธ์เป็น "บทเล่าเรื่อง" เหมือนมีโค้ชส่วนตัวแนะนำ

**Key Constraints:**

  * **Stateless:** ไม่มีการเก็บ Data ลง Database ไม่ต้อง Login
  * **Client-side Calculation:** คำนวณทุกอย่างที่หน้าบ้าน (Frontend)
  * **MVP:** เน้นฟังก์ชันหลัก ใช้งานง่าย จบเร็ว

## 2\. Tech Stack & Tools

  * **Framework:** Next.js 14+ (App Router)
  * **Language:** TypeScript (แนะนำ) หรือ JavaScript
  * **UI Framework:** **Shadcn UI** (Built on Tailwind CSS & Radix UI)
  * **Deployment:** Vercel
  * **Line Integration:** LINE LIFF SDK (สำหรับดึง Display Mode, แต่ Logic หลักทำงานได้แม้ไม่มี SDK)

## 3\. User Journey & Flow

1.  **Landing & Setup:**
      * User เข้ามาเจอหน้าเลือก **ระยะทาง (Distance)**
2.  **Target Setting:**
      * User เลือกโหมดเป้าหมาย: **Pace** หรือ **Total Time**
      * User กรอกตัวเลข
      * กดปุ่ม "Analyze Race Plan"
3.  **The Result (Story Mode):**
      * ระบบแสดงผลลัพธ์เป็น "บทความกลยุทธ์"
      * แสดงตารางสรุป Split
      * แจ้งเตือนจุดให้น้ำ

## 4\. Functional Logic (The Algorithm)

### 4.1 Input Parameters

  * **Distance ($D$):** 5, 10, 21, 42 km
  * **Target Type:** Pace ($P_{target}$) หรือ Time ($T_{target}$)

### 4.2 Core Calculation

**Step 1: หา Base Pace (ช่วง Cruising)**

  * *Condition A (Input = Pace):*
    $$BasePace = P_{target}$$
  * *Condition B (Input = Time):*
    หา Average Pace ก่อน: $P_{avg} = T_{target} / D$
    จากนั้นชดเชยเวลา (Compensate) เพราะแผนเราเริ่มช้า:
    $$BasePace = P_{avg} / 1.01$$
    *(หมายเหตุ: ต้องแปลงหน่วยเป็นวินาทีก่อนคำนวณเสมอ)*

**Step 2: คำนวณ 3 Phases**

1.  **Warm Up:** $BasePace \times 1.125$ (ช้าลง 12.5%)
2.  **Cruise:** $BasePace \times 1.00$ (เท่าเดิม)
3.  **Kick:** $BasePace \times 0.925$ (เร็วขึ้น 7.5%)

**Step 3: แบ่งระยะ (Distance Split)**

  * **Warm Up:** 0 ถึง 20% ของระยะ
  * **Cruise:** 20% ถึง 80% ของระยะ
  * **Kick:** 80% ถึง 100% ของระยะ

## 5\. UI/UX Design with Shadcn UI

เราจะใช้ Components ของ Shadcn UI เพื่อความสวยงามและ Modern ครับ

### 5.1 Theme & Layout

  * **Font:** ภาษาไทยแนะนำ `Kanit` หรือ `Sarabun` (Google Fonts)
  * **Container:** `max-w-md` (Mobile First layout) กลางจอ

### 5.2 Screen Components Detail

#### **Screen 1: Distance Selection**

  * **Component:** `Card`
  * **Content:**
      * Header: "เลือกระยะทางของคุณ"
      * Body: Grid Layout ใส่ปุ่ม `Button` (Variant: Outline หรือ Secondary)
          * `[5K]`, `[10K]`, `[21K]`, `[42K]`
      * *Interaction:* พอกดปุ่ม ให้เก็บ State `distance` แล้ว Auto slide ไปส่วนถัดไป

#### **Screen 2: Input Mode & Data**

  * **Component:** `Tabs` (Shadcn UI)
      * Tab 1: "Target Pace"
      * Tab 2: "Target Time"
  * **Component:** `Input`
      * Mask Input ให้กรอกง่ายๆ เช่น `MM:SS` หรือ `HH:MM:SS`
  * **Component:** `Button` (Size: lg, Class: w-full)
      * Text: "คำนวณแผนการวิ่ง"

#### **Screen 3: Result (The Dashboard)**

  * **Component:** `Card` (สำหรับสรุปเวลาจบ)

      * Title: "เวลาจบที่คาดการณ์ (Estimated Finish)"
      * Content: เวลาตัวเลขใหญ่ๆ (Big Bold Text)

  * **Component:** `Accordion` หรือ `ScrollArea` (สำหรับบทความเล่าเรื่อง)

      * แสดง Text ตาม Content Strategy ด้านล่าง จัด Format ให้อ่านง่าย

  * **Component:** `Table` (Shadcn UI)

      * Headers: ช่วง / ระยะ / Pace
      * Rows: แสดงข้อมูล 3 ช่วง

  * **Component:** `Alert` (Variant: default/info)

      * Icon: หยดน้ำ (Lucide Icon: `Droplet`)
      * Title: "Hydration Plan"
      * Description: "ดื่มน้ำทุกๆ 2km"

-----

## 6\. Content Strategy (Narrative Output)

*ตัวแปรในวงเล็บปีกกา `{...}` คือค่าที่ได้จากการคำนวณ*

### หัวข้อ: แผนพิชิต {Distance}km ของคุณ

#### 🟢 ช่วงที่ 1: "เริ่มให้ช้า"

**ระยะ: กม.ที่ {Start\_Range}**
"เริ่มต้น {Start\_Dist} กิโลเมตรแรก อย่าเพิ่งรีบครับ\! ให้คุณวิ่งด้วยความเร็ว **Pace {Warmup\_Pace}** เท่านั้น ช่วงนี้สำคัญมากที่ต้อง 'ดึง' ตัวเองให้ช้ากว่าความรู้สึก เพื่อวอร์มเครื่องและเก็บพลังงานไว้ใช้ตอนท้าย จำไว้ว่าถ้าคุณรู้สึกว่าวิ่งช้าไป แปลว่าคุณมาถูกทางแล้วครับ"

#### 🟡 ช่วงที่ 2: "รักษาเพซแข่ง"

**ระยะ: กม.ที่ {Cruise\_Range}**
"เมื่อเข้าสู่กิโลเมตรที่ {Cruise\_Start\_KM} ให้ขยับความเร็วขึ้นมาล็อกไว้ที่ **Pace {Cruise\_Pace}** นี่คือ Race Pace จริงของคุณ รักษารอบขาและการหายใจให้นิ่งที่สุด เหมือนหุ่นยนต์ ช่วงนี้ยาวนานที่สุด ให้โฟกัสที่ฟอร์มการวิ่งครับ"

#### 🔴 ช่วงที่ 3: "ใส่ยับถ้าคุณยังเหลือ"

**ระยะ: กม.ที่ {Kick\_Range}**
"ช่วง {Kick\_Dist} กิโลเมตรสุดท้าย ถ้าแรงยังเหลือ นี่คือเวลาของคุณ\! ให้ค่อยๆ เร่งความเร็วขึ้นไปที่ **Pace {Kick\_Pace}** เทคนิคคือ 'มองหาคนข้างหน้า' ล็อกเป้าหมาย แล้วค่อยๆ วิ่งดูดเข้าไปหา แซงแล้วมองหาคนถัดไป ใช้แรงก๊อกสุดท้ายใส่ให้หมดจนเข้าเส้นชัย\!"

-----

## 7\. Developer Implementation Guide (Shadcn Specific)

เพื่อให้ง่ายต่อการ Dev นี่คือ Command ที่ต้องใช้ครับ:

```bash
# 1. Setup Project
npx create-next-app@latest smart-pacer --typescript --tailwind --eslint
cd smart-pacer

# 2. Init Shadcn UI
npx shadcn-ui@latest init
# (Settings: Default, Slate, CSS Variables: Yes)

# 3. Install Required Components
npx shadcn-ui@latest add button card input tabs table alert separator badge
npm install lucide-react date-fns
```

### File Structure แนะนำ

```
app/
├── layout.tsx       # Import Font (Kanit) here
├── page.tsx         # Main Logic Container (State Management here)
└── components/
    ├── step-distance.tsx
    ├── step-input.tsx
    ├── step-result.tsx
    └── ui/          # Shadcn components auto-generated here
utils/
    └── calculator.ts # Logic สูตรคำนวณทั้งหมดอยู่ที่นี่
```

-----

### จุดเน้นย้ำสำหรับ MVP

1.  **No Validation:** ไม่ต้องเขียน Validation ซับซ้อนถ้า user กรอกผิดเล็กน้อย (เช่น กรอกวินาทีเกิน 60) ให้ระบบปัดเศษให้เองเลย เพื่อลดความยุ่งยาก
2.  **UX Focus:** ตอนกดปุ่ม "คำนวณ" ให้มี Animation หมุนๆ นิดหน่อย (Loading state) เพื่อให้ดู Professional ก่อนโชว์ผลลัพธ์